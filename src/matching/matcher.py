"""
matcher.py — Core fingerprint matching engine.

Stub module — implementation in Phase 4.
"""

# TODO: Implement minutia pairing and matching (Phase 4)
# - For each probe minutia, find nearest gallery minutia within tolerance
# - Compute matching score = matched pairs / min(probe count, gallery count)
