"""
main.py — FastAPI application for fingerprint verification service.

Stub module — implementation in Phase 6.
"""

# TODO: Implement FastAPI endpoints (Phase 6)
# - POST /enroll — accepts image + user_id, stores template
# - POST /verify — accepts probe image + claimed user_id, runs matching
